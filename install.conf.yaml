- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim/lua:
      create: true
      path: config/nvim/lua
    ~/.zshrc: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
